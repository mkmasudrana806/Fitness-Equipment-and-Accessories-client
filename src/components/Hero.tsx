import heroImg from "../assets/images/hero.jpg";

const Hero = () => {
  return (
    <div style={{ width: "100%" }}>
      <img width={"100%"} src={heroImg} alt="" />
    </div>
  );
};

export default Hero;
